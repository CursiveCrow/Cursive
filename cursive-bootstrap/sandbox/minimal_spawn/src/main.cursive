// Minimal spawn test - no captures, just compute
public procedure main(ctx: Context) -> i32 {
  let domain = ctx~>inline()

  parallel domain {
    let _ = wait spawn { 42 }
  }

  // If we get here without crashing, return 42
  return 42i32
}
