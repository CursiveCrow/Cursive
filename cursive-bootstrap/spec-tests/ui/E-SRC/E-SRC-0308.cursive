// RUN: compile-fail
// DIAG: E-SRC-0308
// SPEC: 3.2
// DESC: Lexically sensitive Unicode character outside unsafe block
// SKIP: Requires bidi control characters in source

// NOTE: This test requires bidi control chars (LexSecure violation).

public procedure main(move ctx: Context) -> i32 {
    return 0
}
