// RUN: compile-fail
// DIAG: E-CON-0121
// SPEC: 8.9
// DESC: Move of already-moved binding

record Resource {
    value: i32
}

procedure double_move() -> () {
    let r: Resource = Resource { value: 42 }
    parallel {
        spawn {
            let a: Resource = move r
        }
        spawn {
            let b: Resource = move r  //~ ERROR E-CON-0121: already moved
        }
    }
}

public procedure main(move ctx: Context) -> i32 {
    return 0
}
