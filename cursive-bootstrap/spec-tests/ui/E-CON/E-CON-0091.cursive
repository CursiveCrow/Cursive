// RUN: compile-fail
// DIAG: E-CON-0091
// SPEC: 8.9
// DESC: Write to path outside keyed set in speculative block

record A {
    var x: i32
}

record B {
    var y: i32
}

procedure write_outside_keyed(a: shared A, b: shared B) -> () {
    # speculative write a {
        b.y = 10  //~ ERROR E-CON-0091: write outside keyed set
    }
}

public procedure main(move ctx: Context) -> i32 {
    return 0
}
