// RUN: run-pass
// EXIT: 42
// SPEC: 5.2.2
// DESC: Unique permission binding with move

public procedure consume(move x: unique i32) -> i32 {
    return x
}

public procedure main(move ctx: Context) -> i32 {
    let x: unique i32 = 42
    return consume(move x)
}
