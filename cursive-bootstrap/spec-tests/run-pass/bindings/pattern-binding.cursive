// RUN: run-pass
// EXIT: 42
// SPEC: 5.2.4
// DESC: Pattern binding with record destructuring

record Point {
    x: i32
    y: i32
}

public procedure main(move ctx: Context) -> i32 {
    let p: Point = Point { x: 20, y: 22 }
    let Point { x, y }: Point = p
    return x + y
}
