// RUN: run-pass
// EXIT: 42
// SPEC: 5.2.11
// DESC: Nested record access

record Inner {
    value: i32
}

record Outer {
    inner: Inner
}

public procedure main(move ctx: Context) -> i32 {
    let o: Outer = Outer { inner: Inner { value: 42 } }
    return o.inner.value
}
