# SPEC_COV: Lower-Dispatch
# SPEC_COV: IR-Dispatch-Run

public procedure main(ctx: Context) -> i32 {
  let domain: $ExecutionDomain = ctx~>cpu()
  parallel domain {
    dispatch i in 0..100 {
      // Parallel iteration
    }
  }
  return 0
}
