# SPEC_COV: Lower-Parallel
# SPEC_COV: IR-Parallel-Begin
# SPEC_COV: IR-Parallel-Join

public procedure main(ctx: Context) -> i32 {
  let domain: $ExecutionDomain = ctx~>cpu()
  parallel domain {
    // Empty parallel block
  }
  return 0
}
