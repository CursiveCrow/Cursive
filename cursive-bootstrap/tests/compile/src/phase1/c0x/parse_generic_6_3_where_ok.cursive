# SPEC_COV: C0X-6.3-Where-Parse
// §6.3: where clause with bounds

class Printable { }

record Wrapper<T> where T <: Printable { value: T }

public procedure main(ctx: Context) -> i32 {
  return 0
}
