# SPEC_COV: C0X-14.4-ContractFull-Parse
// §14.4: Contract clause |= P => Q (full contract)

procedure full_contract(x: i32) -> i32
  |= x > 0 => @result > 0
{
  return x
}

public procedure main(ctx: Context) -> i32 {
  return 0
}
