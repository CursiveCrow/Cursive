# SPEC_COV: C0X-7.2-CoarsenField-Parse
// §7.2: Coarsening marker # on field segment

record Inner { val: i32 }
record Outer { inner: Inner }

public procedure main(ctx: Context) -> i32 {
  var o: shared Outer = Outer { inner: Inner { val: 42 } }
  # o.#inner {
    return o.inner.val
  }
  return 0
}
