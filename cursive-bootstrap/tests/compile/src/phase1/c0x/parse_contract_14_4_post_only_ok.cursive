# SPEC_COV: C0X-14.4-ContractPost-Parse
// §14.4: Contract clause |= => Q (postcondition only)

procedure post_only(x: i32) -> i32
  |= => @result >= 0
{
  if x < 0 { return 0 - x }
  return x
}

public procedure main(ctx: Context) -> i32 {
  return 0
}
