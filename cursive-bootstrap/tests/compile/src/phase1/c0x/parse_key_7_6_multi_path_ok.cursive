# SPEC_COV: C0X-7.6-KeyBlockMultiPath-Parse
// §7.6: Key block with multiple paths

record A { val: i32 }
record B { val: i32 }

public procedure main(ctx: Context) -> i32 {
  var a: shared A = A { val: 1 }
  var b: shared B = B { val: 2 }
  # a, b {
    return a.val + b.val
  }
  return 0
}
