# SPEC_COV: C0X-Attr-Dynamic-Parse
// Attribute [[dynamic]] on procedure

[[dynamic]]
procedure runtime_checked(x: i32) -> i32
  |= x > 0
{
  return x
}

public procedure main(ctx: Context) -> i32 {
  return 0
}
