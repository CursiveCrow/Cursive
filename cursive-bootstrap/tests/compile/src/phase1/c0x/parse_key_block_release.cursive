# SPEC_COV: Parse-KeyBlock
# SPEC_COV: Parse-KeyBlockMod-Release
// Tests parsing of key block with release modifier.

record Data { value: i32 }

public procedure main(ctx: Context) -> i32 {
  var d: shared Data = Data { value: 42 }
  # d write {
    # d release read {
      // Inner block with released read
    }
  }
  return d.value
}
