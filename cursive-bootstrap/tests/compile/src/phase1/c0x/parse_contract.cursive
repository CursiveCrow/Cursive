# SPEC_COV: Parse-Contract
# SPEC_COV: Parse-ContractClause
// Tests parsing of contract clauses |= pre => post.

procedure with_pre(x: i32) -> i32
  |= x > 0
{
  return x
}

procedure with_post(x: i32) -> i32
  |= => @result >= 0
{
  return x
}

procedure with_both(x: i32) -> i32
  |= x >= 0 => @result > x
{
  return x + 1
}

public procedure main(ctx: Context) -> i32 {
  return 0
}
