# SPEC_COV: C0X-14.5.4-Entry-Parse
// §14.5.4: @entry(expr) syntax in postcondition

procedure with_entry(x: i32) -> i32
  |= => @result == @entry(x) + 1
{
  return x + 1
}

public procedure main(ctx: Context) -> i32 {
  return 0
}
