# SPEC_COV: Parse-KeyBlock
# SPEC_COV: Parse-KeyBlockMod-Dynamic
// Tests parsing of key block with dynamic modifier.

record Data { value: i32 }

public procedure main(ctx: Context) -> i32 {
  var d: shared Data = Data { value: 42 }
  # d dynamic write {
    d.value = 100
  }
  return d.value
}
