# SPEC_COV: Parse-KeyBoundary
# SPEC_COV: Parse-KeyBoundary-Modal
// Tests parsing of key boundary marker on modal state fields.

modal Counter {
  @Ready { count: i32, #internal: i32 }
  @Done { result: i32 }
}

public procedure main(ctx: Context) -> i32 {
  return 0
}
