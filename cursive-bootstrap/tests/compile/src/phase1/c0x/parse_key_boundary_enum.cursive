# SPEC_COV: Parse-KeyBoundary
# SPEC_COV: Parse-KeyBoundary-Enum
// Tests parsing of key boundary marker on enum payload fields.

enum Result {
  Ok(value: i32)
  Err(#code: i32, message: i32)
}

public procedure main(ctx: Context) -> i32 {
  return 0
}
