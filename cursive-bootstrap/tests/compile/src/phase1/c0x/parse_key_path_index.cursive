# SPEC_COV: Parse-KeyPath
# SPEC_COV: Parse-KeyPath-Index
// Tests parsing of key path with index segments.

public procedure main(ctx: Context) -> i32 {
  var arr: shared [i32; 3] = [1, 2, 3]
  # arr[0] {
    return arr[0]
  }
  return 0
}
