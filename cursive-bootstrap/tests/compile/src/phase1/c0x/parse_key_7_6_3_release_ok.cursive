# SPEC_COV: C0X-7.6.3-KeyBlockRelease-Parse
// §7.6.3: Key block with release modifier

record Data { value: i32 }

public procedure main(ctx: Context) -> i32 {
  var d: shared Data = Data { value: 42 }
  # d write {
    d.value = 100
    # d release write {
      // Keys temporarily released
    }
    d.value = d.value + 1
  }
  return d.value
}
