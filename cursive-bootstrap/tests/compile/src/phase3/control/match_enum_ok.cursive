# SPEC_COV: T-Match
// Match on union type with enum-like variants

public procedure main(ctx: Context) -> i32 {
  let value: i32 = 1
  let x: i32 = match value {
    1 => 10,
    2 => 20,
    _ => 0
  }
  return x
}
