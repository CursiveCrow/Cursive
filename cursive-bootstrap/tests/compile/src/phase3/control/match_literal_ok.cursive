# SPEC_COV: T-Match
// Match with literal patterns

public procedure main(ctx: Context) -> i32 {
  let x: i32 = 5;
  let y: i32 = match x {
    0 => 100,
    1 => 200,
    _ => 300
  };
  return y;
}
