# SPEC_COV: T-Match
// Nested match expressions

public procedure main(ctx: Context) -> i32 {
  let a: bool = true;
  let b: bool = false;
  let x: i32 = match a {
    true => match b { true => 1, false => 2 },
    false => 3
  };
  return x;
}
