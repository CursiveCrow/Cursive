# SPEC_COV: T-Match
// Match with guard clauses

public procedure main(ctx: Context) -> i32 {
  let x: i32 = 15
  let y: i32 = match x {
    v if v > 10 => 100,
    v if v > 5 => 50,
    _ => 0
  }
  return y
}
