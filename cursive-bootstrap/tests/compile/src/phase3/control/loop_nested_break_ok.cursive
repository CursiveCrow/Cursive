# SPEC_COV: T-Break
// Nested loop break

public procedure main(ctx: Context) -> i32 {
  var outer: i32 = 0
  loop {
    outer = outer + 1
    var inner: i32 = 0
    loop {
      inner = inner + 1
      if inner > 3 { break }
    }
    if outer > 2 { break }
  }
  return outer
}
