# SPEC_COV: T-Match
// Match on boolean as modal-like

public procedure main(ctx: Context) -> i32 {
  let ready: bool = false
  let x: i32 = match ready {
    true => 1,
    false => 0
  }
  return x
}
