# SPEC_COV: E-PAT-2781
// E-PAT-2781: Pattern type mismatch

public procedure main(ctx: Context) -> i32 {
  let u: i32 | bool = 42
  let x: i32 = match u {
    v: i32 => v,
    c: char => 0
  }
  return x
}
