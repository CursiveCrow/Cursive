# SPEC_COV: T-Assign
# SPEC_COV: E-MOD-2401
// E-MOD-2401: Cannot assign through const path

procedure try_modify(x: const i32) -> () {
  x = 10
}

public procedure main(ctx: Context) -> i32 {
  let v: i32 = 5
  try_modify(v)
  return 0
}
