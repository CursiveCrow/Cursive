# SPEC_COV: T-Modal-Widen
# SPEC_COV: T-Modal-Widen-Perm
modal Mod { @Open { v: i32 } }

public procedure main(ctx: Context) -> i32 {
  let a = Mod@Open { v: 1 };
  let b: Mod = widen (move a);
  let c: const Mod@Open = unsafe { transmute::<i32, const Mod@Open>(0) };
  let d: const Mod = widen (move c);
  return 0;
}
