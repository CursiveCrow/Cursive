# SPEC_COV: B-Move-Field-NonUnique-Err
// E-MEM-3004: Partial move from non-unique binding
record R {
  f: i32
}
public procedure main(ctx: Context) -> i32 {
  let r: R = R { f: 1 };
  let x: i32 = move r.f;
  return 0;
}
