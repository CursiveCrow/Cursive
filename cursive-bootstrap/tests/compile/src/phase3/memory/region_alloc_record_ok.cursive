# SPEC_COV: Region-AllocRecord
# SPEC_COV: T-RegionAlloc
// Region allocation of primitive type.
// Tests allocation in regions.

public procedure main(ctx: Context) -> i32 {
  region {
    let p = ^42;
    p;
  }
  return 0;
}
