# SPEC_COV: Ptr-Deref
# SPEC_COV: T-Deref
// Pointer dereference for read.
// Tests pointer read operations.

public procedure main(ctx: Context) -> i32 {
  var x: i32 = 42;
  let p: Ptr<i32>@Valid = &x;
  return *p;
}
