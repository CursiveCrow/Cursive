# SPEC_COV: B-Move-Field-Moved-Err
// E-MEM-3001: Partial move of already-moved field
record R {
  f: i32
}
public procedure main(ctx: Context) -> i32 {
  var r: unique R = R { f: 1 };
  let a: i32 = move r.f;
  let b: i32 = move r.f;
  return 0;
}
