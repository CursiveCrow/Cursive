# SPEC_COV: T-Region
// Region lifetime - allocation valid within region

public procedure main(ctx: Context) -> i32 {
  var result: i32 = 0
  region {
    let p = ^42
    result = *p
  }
  return result
}
