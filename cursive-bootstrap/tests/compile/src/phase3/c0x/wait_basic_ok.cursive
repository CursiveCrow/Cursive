# SPEC_COV: C0X-Wait-Basic
# SPEC_COV: T-Wait
// Tests basic wait syntax and typing (§2.7.3, §10.3)
// Expected: no diagnostics

public procedure main(ctx: Context) -> i32 {
  let domain: $ExecutionDomain = ctx~>cpu()
  let res = parallel domain {
    let h = spawn { 42 }
    wait h
  }
  return res
}
