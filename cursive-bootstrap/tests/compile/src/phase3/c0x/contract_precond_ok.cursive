# SPEC_COV: C0X-Contract-Precondition
# SPEC_COV: C0X-Contract-Syntax
// Tests procedure precondition contract clause syntax.

procedure positive_only(x: i32) -> i32
  |= x > 0
{
  return x * 2
}

public procedure main(ctx: Context) -> i32 {
  var val: i32 = 5
  return positive_only(val)
}
