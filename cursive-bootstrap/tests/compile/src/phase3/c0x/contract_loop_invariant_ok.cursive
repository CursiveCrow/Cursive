# SPEC_COV: C0X-Contract-LoopInvariant
# SPEC_COV: C0X-Contract-LoopWhere
// Tests loop invariant with where clause.

public procedure main(ctx: Context) -> i32 {
  var sum: i32 = 0
  var i: i32 = 0
  loop i < 10 where { i >= 0 && i <= 10 } {
    sum = sum + i
    i = i + 1
  }
  return sum
}
