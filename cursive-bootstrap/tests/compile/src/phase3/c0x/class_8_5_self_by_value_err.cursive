# SPEC_COV: C0X-8.5-SelfByValue
# SPEC_COV: E-SEM-2618
// §8.5: Self by value in param or return makes method non-vtable-eligible.
// Expected diagnostic: E-SEM-2618 (if class not marked dispatchable)

class ClonesItself {
  procedure clone_self(~) -> Self
}

public procedure main(ctx: Context) -> i32 {
  return 0
}
