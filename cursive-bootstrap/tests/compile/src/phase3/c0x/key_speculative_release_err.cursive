# SPEC_COV: C0X-Key-Speculative-NoRelease
# SPEC_COV: E-CON-0094
// Tests that speculative cannot be combined with release.
// Expected diagnostic: E-CON-0094

record Data { value: i32 }

public procedure main(ctx: Context) -> i32 {
  var d: shared Data = Data { value: 42 }
  # d speculative release write {
    // Error: speculative + release invalid
  }
  return d.value
}
