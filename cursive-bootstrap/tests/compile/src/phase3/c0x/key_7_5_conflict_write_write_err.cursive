# SPEC_COV: C0X-7.5-ConflictWriteWrite
# SPEC_COV: E-SEM-2701
// §7.5: Overlapping paths with at least one Write conflict.
// Expected diagnostic: E-SEM-2701 (key conflict)

record Data { value: i32 }

public procedure main(ctx: Context) -> i32 {
  var d: shared Data = Data { value: 42 }
  # d write {
    # d write {
      // d overlaps d, both Write => conflict
      d.value = 100
    }
  }
  return 0
}
