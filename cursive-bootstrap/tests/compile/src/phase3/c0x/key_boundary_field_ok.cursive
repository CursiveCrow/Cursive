# SPEC_COV: C0X-Key-Boundary
# SPEC_COV: C0X-Key-TypeLevel
// Tests type-level key boundary marker (#) on record fields.

record Encapsulated {
  public_data: i32
  #private_impl: i32
}

public procedure main(ctx: Context) -> i32 {
  var e: Encapsulated = Encapsulated { public_data: 1, #private_impl: 2 }
  return e.public_data
}
