# SPEC_COV: C0X-Contract-TypeInvariant
# SPEC_COV: C0X-Contract-Invariant
// Tests type invariant on record.

record PositiveCounter { value: i32 }
where { self.value >= 0 }

public procedure main(ctx: Context) -> i32 {
  var c: PositiveCounter = PositiveCounter { value: 10 }
  return c.value
}
