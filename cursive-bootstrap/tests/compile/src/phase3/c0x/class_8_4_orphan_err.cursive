# SPEC_COV: C0X-8.4-Orphan
# SPEC_COV: E-SEM-2616
// §8.4: Neither T nor Cl local => orphan rule violation.
// Expected diagnostic: E-SEM-2616

// Assuming ForeignClass and ForeignType are from other assemblies
// This test simulates the error condition

using foreign::ForeignClass
using foreign::ForeignType

ForeignType <: ForeignClass {
  procedure method(~) -> i32 { return 0 }
}

public procedure main(ctx: Context) -> i32 {
  return 0
}
