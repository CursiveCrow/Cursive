# SPEC_COV: C0X-7.2-KeyPathField
# SPEC_COV: KeyPath-Segment
// §7.2: Key path segment can be field access .f

record Inner { val: i32 }
record Outer { inner: Inner }

public procedure main(ctx: Context) -> i32 {
  var o: shared Outer = Outer { inner: Inner { val: 42 } }
  // Key path: (o, .inner, .val)
  # o.inner.val {
    return o.inner.val
  }
  return 0
}
