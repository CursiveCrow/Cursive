# SPEC_COV: C0X-7.5-ConflictReadWrite
# SPEC_COV: E-SEM-2701
// §7.5: Overlapping paths conflict if one is Write.
// Expected diagnostic: E-SEM-2701 (key conflict)

record Data { value: i32 }

public procedure main(ctx: Context) -> i32 {
  var d: shared Data = Data { value: 42 }
  # d {
    # d write {
      // d (Read) and d (Write) overlap => conflict
      d.value = 100
    }
  }
  return 0
}
