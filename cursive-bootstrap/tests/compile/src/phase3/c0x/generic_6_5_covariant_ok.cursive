# SPEC_COV: C0X-6.5-Covariant
# SPEC_COV: Variance-Covariant
// §6.5: Type parameter in immutable storage contributes Covariant.

record ReadOnly<T> { value: const T }

// const ReadOnly<Child> <: const ReadOnly<Parent> when Child <: Parent

record Animal { name: i32 }
record Cat { name: i32, meow: bool }

public procedure main(ctx: Context) -> i32 {
  var r: const ReadOnly<i32> = ReadOnly<i32> { value: 42 }
  return r.value
}
