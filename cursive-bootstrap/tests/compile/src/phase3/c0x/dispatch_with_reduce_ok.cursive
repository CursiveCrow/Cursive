# SPEC_COV: C0X-Dispatch-Reduce
# SPEC_COV: T-Dispatch-Reduce
// Tests dispatch with reduce option (§18.5.3)
// Expected: no diagnostics

public procedure main(ctx: Context) -> i32 {
  let domain: $ExecutionDomain = ctx~>cpu()
  parallel domain {
    dispatch i in 0..100 [reduce: +] {
      i as i32
    }
  }
  return 0
}
