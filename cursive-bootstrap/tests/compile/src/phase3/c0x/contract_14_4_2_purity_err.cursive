# SPEC_COV: C0X-14.4.2-PurityViolation
# SPEC_COV: E-SEM-2803
// §14.4.2: Contract predicates MUST be pure.
// Expected diagnostic: E-SEM-2803 (impure predicate in contract)

procedure impure_contract(fs: $FileSystem, x: i32) -> i32
  |= fs ~> exists("test.txt")
{
  return x
}

public procedure main(ctx: Context) -> i32 {
  return 0
}
