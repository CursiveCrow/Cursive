# SPEC_COV: C0X-Dispatch-Unit-Type
# SPEC_COV: T-Dispatch
// Tests dispatch without reduce returns unit (§18.5.1)
// Γ ⊢ range : Range⟨I⟩    Γ, i : I ⊢ B : T
// ──────────────────────────────────────────────────────────
// Γ ⊢ `dispatch` i `in range` {B} : ()
// Expected: no diagnostics

public procedure main(ctx: Context) -> i32 {
  let domain: $ExecutionDomain = ctx~>cpu()
  parallel domain {
    // Without reduce, dispatch returns unit
    dispatch i in 0..100 {
      // Side effect only, no return value collected
    }
  }
  return 0
}
