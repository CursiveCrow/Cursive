# SPEC_COV: C0X-Generics-Procedure
# SPEC_COV: C0X-Generics-TypeParam
// Tests generic procedure with type parameter.

procedure identity<T>(x: T) -> T
where T <: Bitcopy
{
  return x
}

public procedure main(ctx: Context) -> i32 {
  var val: i32 = 42
  return identity(val)
}
