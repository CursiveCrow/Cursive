# SPEC_COV: C0X-Parallel-Mixed
# SPEC_COV: §18.1.3
// Tests parallel with mixed spawns and reducing dispatch (§18.1.3)
// Mixed spawns and reducing dispatch yields tuple of all results
// Expected: no diagnostics

public procedure main(ctx: Context) -> i32 {
  let domain: $ExecutionDomain = ctx~>cpu()
  
  parallel domain {
    spawn { 10 }
    dispatch i in 0..100 [reduce: +] {
      1
    }
  }
  
  return 0
}
