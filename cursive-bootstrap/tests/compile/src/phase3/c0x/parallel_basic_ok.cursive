# SPEC_COV: C0X-Parallel-Basic
# SPEC_COV: T-Parallel
// Tests basic parallel block syntax and typing (§2.7.1, §18.1.1)
// Expected: no diagnostics

public procedure main(ctx: Context) -> i32 {
  let domain: $ExecutionDomain = ctx~>cpu()
  parallel domain {
    // Empty parallel block
  }
  return 0
}
