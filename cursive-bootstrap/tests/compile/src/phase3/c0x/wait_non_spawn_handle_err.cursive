# SPEC_COV: C0X-Wait-Type-Check
# SPEC_COV: E-CON-0161
// Tests that wait requires SpawnHandle type (§10.3)
// Expected diagnostic: E-CON-0161

public procedure main(ctx: Context) -> i32 {
  let not_handle: i32 = 42
  // Error: wait on non-SpawnHandle type
  let res: i32 = wait not_handle
  return res
}
