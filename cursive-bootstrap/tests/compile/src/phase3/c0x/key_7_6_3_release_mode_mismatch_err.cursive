# SPEC_COV: C0X-7.6.3-ReleaseMismatch
# SPEC_COV: E-SEM-2704
// §7.6.3: Release block mode MUST match outer mode for released paths.
// Expected diagnostic: E-SEM-2704

record Data { value: i32 }

public procedure main(ctx: Context) -> i32 {
  var d: shared Data = Data { value: 42 }
  # d {
    // Outer is Read mode
    # d release write {
      // Release with Write mode, but outer is Read => mismatch
    }
  }
  return 0
}
