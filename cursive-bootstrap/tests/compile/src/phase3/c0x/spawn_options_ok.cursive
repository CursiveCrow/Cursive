# SPEC_COV: C0X-Spawn-Options
# SPEC_COV: §18.4.1
// Tests spawn with options (§18.4.1)
// Note: Simplified - full option support pending
// Expected: no diagnostics

public procedure main(ctx: Context) -> i32 {
  let domain: $ExecutionDomain = ctx~>cpu()
  parallel domain {
    // Basic spawn (options not fully implemented)
    let h1 = spawn { 42 }
    let h2 = spawn { 100 }
    let v1 = wait h1
    let v2 = wait h2
  }
  return 0
}
