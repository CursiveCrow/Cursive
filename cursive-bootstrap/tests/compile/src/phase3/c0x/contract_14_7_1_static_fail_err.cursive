# SPEC_COV: C0X-14.7.1-StaticFail
# SPEC_COV: E-SEM-2801
// §14.7.1: If StaticProof fails and not in [[dynamic]] context => ill-formed.
// Expected diagnostic: E-SEM-2801

procedure unprovable(x: i32) -> i32
  |= => @result > 100
{
  return x
}

public procedure main(ctx: Context) -> i32 {
  var a: i32 = 5
  return unprovable(a)
}
