# SPEC_COV: C0X-Parallel-Empty
# SPEC_COV: T-Parallel-Empty
// Tests empty parallel block has unit type (§18.1.3)
// Γ ⊢ `parallel` D {} : ()
// Expected: no diagnostics

public procedure main(ctx: Context) -> i32 {
  let domain: $ExecutionDomain = ctx~>cpu()
  // Empty parallel block returns unit
  parallel domain {
  }
  return 0
}
