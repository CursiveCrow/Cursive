# SPEC_COV: C0X-Dispatch-Ordered
# SPEC_COV: §18.5.2
# SPEC_COV: §18.8.1
// Tests dispatch with [ordered] attribute (§18.5.2, §18.8.1)
// Forces sequential side-effect ordering
// Expected: no diagnostics

public procedure main(ctx: Context) -> i32 {
  let domain: $ExecutionDomain = ctx~>cpu()
  parallel domain {
    // [ordered] ensures side effects are applied in index order
    dispatch i in 0..100 [ordered] {
    }
  }
  return 0
}
