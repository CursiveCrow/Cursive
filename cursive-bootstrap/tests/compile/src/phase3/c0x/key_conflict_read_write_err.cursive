# SPEC_COV: C0X-Key-Conflict-ReadWrite
# SPEC_COV: E-CON-0010
// Tests that read-write conflict on overlapping paths is an error.
// Expected diagnostic: E-CON-0010

record Data { value: i32 }

public procedure main(ctx: Context) -> i32 {
  var d: shared Data = Data { value: 42 }
  # d {
    # d write {
      // Error: read-write conflict on same path
      d.value = 100
    }
  }
  return d.value
}
