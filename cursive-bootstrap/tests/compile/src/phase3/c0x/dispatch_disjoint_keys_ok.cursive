# SPEC_COV: C0X-Dispatch-Disjoint-Keys
# SPEC_COV: K-Disjoint-Dispatch
// Tests key disjointness guarantee for dispatch (§18.5.3)
// `dispatch` v `in` r { … a[v] … }
// ────────────────────────────────────────────────────────────
// ∀ v_1, v_2 ∈ r, v_1 ≠ v_2 ⇒ ProvablyDisjoint(a[v_1], a[v_2])
// Expected: no diagnostics

public procedure main(ctx: Context) -> i32 {
  let domain: $ExecutionDomain = ctx~>cpu()
  parallel domain {
    // Each iteration accesses disjoint index - fully parallel
    dispatch i in 0..100 {
    }
  }
  return 0
}
