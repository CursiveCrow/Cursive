# SPEC_COV: C0X-Key-ReleaseMismatch
# SPEC_COV: E-CON-0018
// Tests that release mode must match outer mode.
// Expected diagnostic: E-CON-0018

record Data { value: i32 }

public procedure main(ctx: Context) -> i32 {
  var d: shared Data = Data { value: 42 }
  # d {
    # d release write {
      // Error: trying to release write in read block
    }
  }
  return d.value
}
