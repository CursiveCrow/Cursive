# SPEC_COV: C0X-9.2-ImportAs
# SPEC_COV: Import-Alias
// §9.2: import with explicit alias via as.

import cursive::runtime as rt

public procedure main(ctx: Context) -> i32 {
  // rt is alias for cursive::runtime
  return 0
}
