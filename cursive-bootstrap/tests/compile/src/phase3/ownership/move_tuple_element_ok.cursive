# SPEC_COV: Move-TupleElement
# SPEC_COV: B-Move-Field
// Move from tuple element with unique permission.
// Tests move semantics on tuple index access.

public procedure main(ctx: Context) -> i32 {
  let t: unique (i32, i32) = unsafe { transmute::<(i32, i32), unique (i32, i32)>((10, 20)) };
  let x: unique i32 = move t.0;
  return 0;
}
