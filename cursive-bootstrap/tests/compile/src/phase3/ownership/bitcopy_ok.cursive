# SPEC_COV: BitcopyDrop-Ok
record R <: Bitcopy, Clone {
  v: i32,
  procedure clone(~) -> Self { return R { v: self.v }; }
}

public procedure main(ctx: Context) -> i32 {
  let r = R { v: 1 };
  let c = r~>clone();
  return c.v;
}
