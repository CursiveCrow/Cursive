# SPEC_COV: Move-Chain
# SPEC_COV: B-Move-Whole
// Move through chain of bindings.
// Tests that responsibility transfers correctly through multiple moves.

public procedure main(ctx: Context) -> i32 {
  let a: i32 = 1;
  let b: i32 = move a;
  let c: i32 = move b;
  return c;
}
