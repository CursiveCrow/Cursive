# SPEC_COV: B-Inactive
// Path reactivation after pointer expires

public procedure main(ctx: Context) -> i32 {
  var x: i32 = 10
  {
    let p: Ptr<unique i32>@Valid = &x
    *p = 20
  }
  return x
}
