# SPEC_COV: Perm-Array-Element-Err
# SPEC_COV: P-Array-Perm-Propagate
// E-MOD-2401: Cannot mutate array element through const binding.
// const permission prevents element mutation.

public procedure main(ctx: Context) -> i32 {
  let arr: [i32; 3] = [1, 2, 3];
  arr[0usize] = 10;
  return 0;
}
