# SPEC_COV: Perm-Downgrade
# SPEC_COV: Sub-Perm
// Permission can downgrade from unique to const.
// Tests permission lattice subtyping.

procedure read_value(x: i32) -> i32 {
  return x;
}

public procedure main(ctx: Context) -> i32 {
  let v: i32 = 42;
  return read_value(v);
}
