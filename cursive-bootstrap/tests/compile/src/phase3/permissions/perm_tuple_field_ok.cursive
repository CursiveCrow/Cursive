# SPEC_COV: Perm-Tuple-Field
# SPEC_COV: P-Tuple-Perm-Propagate
// Permission propagates to tuple elements for mutation.
// var binding allows tuple element mutation.

public procedure main(ctx: Context) -> i32 {
  var t: (i32, i32) = (1, 2);
  t.0 = 10;
  t.1 = 20;
  return t.0 + t.1;
}
