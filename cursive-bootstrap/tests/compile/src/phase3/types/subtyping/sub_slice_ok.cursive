# SPEC_COV: Sub-Slice
// Slice element type subtyping

procedure takes_slice(s: [i32]) -> i32 {
  return 0;
}

public procedure main(ctx: Context) -> i32 {
  let arr: [i32; 3] = [1, 2, 3];
  let sl: [i32] = arr;
  return takes_slice(sl);
}
