# SPEC_COV: Sub-Func
# SPEC_COV: Sub-Func-Contravariant
// Function parameter contravariance: (const T) -> R <: (unique T) -> R

procedure takes_const(x: const i32) -> i32 { return 0; }

public procedure main(ctx: Context) -> i32 {
  let f: (i32) -> i32 = takes_const;
  return 0;
}
