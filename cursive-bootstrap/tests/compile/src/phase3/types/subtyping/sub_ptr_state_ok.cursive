# SPEC_COV: Sub-Ptr-State
// Pointer state subtyping: Valid <: (Valid | Null)

procedure takes_nullable(p: Ptr<i32>@Valid | Ptr<i32>@Null) -> i32 {
  return 0;
}

public procedure main(ctx: Context) -> i32 {
  var x: i32 = 42;
  let p: Ptr<i32>@Valid = &x;
  return takes_nullable(p);
}
