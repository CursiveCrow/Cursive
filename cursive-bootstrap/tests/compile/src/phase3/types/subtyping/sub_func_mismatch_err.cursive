# SPEC_COV: Sub-Func
# SPEC_COV: E-TYP-1602
// E-TYP-1602: Incompatible function types - wrong arity

procedure one_arg(x: i32) -> i32 { return x; }

public procedure main(ctx: Context) -> i32 {
  let f: (i32, i32) -> i32 = one_arg;
  return 0;
}
