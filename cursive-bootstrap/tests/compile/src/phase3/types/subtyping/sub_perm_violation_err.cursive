# SPEC_COV: Sub-Perm
# SPEC_COV: E-TYP-1602
// E-TYP-1602: Cannot coerce const to unique

procedure takes_unique(x: unique i32) -> i32 {
  return 0;
}

public procedure main(ctx: Context) -> i32 {
  let c: const i32 = 42;
  return takes_unique(c);
}
