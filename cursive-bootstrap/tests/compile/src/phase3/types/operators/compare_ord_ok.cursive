# SPEC_COV: T-Compare
# SPEC_COV: T-Le
# SPEC_COV: T-Gt
# SPEC_COV: T-Ge
// Ordering comparison operators

public procedure main(ctx: Context) -> i32 {
  let a: i32 = 10;
  let b: i32 = 20;
  let le: bool = a <= b;
  let gt: bool = a > b;
  let ge: bool = a >= b;
  return 0;
}
