# SPEC_COV: Union-DirectAccess-Err
// E-TYP-2202: Direct access on union value without pattern matching
record R { x: i32 }

public procedure main(ctx: Context) -> i32 {
  let u: i32 | R = R { x: 1 };
  let x: i32 = u.x;
  return 0;
}
