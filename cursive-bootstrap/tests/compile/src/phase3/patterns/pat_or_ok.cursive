# SPEC_COV: Pat-Or
// Or-pattern matching

public procedure main(ctx: Context) -> i32 {
  let x: i32 = 5
  let y: i32 = match x {
    1 | 2 | 3 => 10,
    4 | 5 | 6 => 20,
    _ => 0
  }
  return y
}
