# SPEC_COV: T-Pat-Type
// Pattern with type annotation

public procedure main(ctx: Context) -> i32 {
  let u: i32 | bool = 42
  let x: i32 = match u {
    v: i32 => v,
    _: bool => 0
  }
  return x
}
