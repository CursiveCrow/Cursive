# SPEC_COV: T-Pat-Range
# SPEC_COV: E-PAT-2721
// E-PAT-2721: Invalid range - start > end

public procedure main(ctx: Context) -> i32 {
  let x: i32 = 5
  let y: i32 = match x {
    10..0 => 1,
    _ => 0
  }
  return y
}
