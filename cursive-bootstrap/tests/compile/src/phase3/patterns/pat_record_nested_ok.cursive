# SPEC_COV: T-Pat-Record
// Nested record destructuring

record Inner { v: i32 }
record Outer { inner: Inner, n: i32 }

public procedure main(ctx: Context) -> i32 {
  let o: Outer = Outer { inner: Inner { v: 5 }, n: 10 }
  return o.n
}
