# SPEC_COV: RangePattern-Empty
// E-SEM-2722: Range pattern start exceeds end (empty range).
// The range 5..3 is empty and invalid.
public procedure main(ctx: Context) -> i32 {
  let x: i32 = 4;
  let result: i32 = match x {
    5i32..3i32 => 1i32,
    _ => 0i32
  };
  return result;
}
