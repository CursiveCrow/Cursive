# SPEC_COV: Match-Union-NonExhaustive
// E-SEM-2705: match expression is not exhaustive for union type.
// Missing a case for the union type.
public procedure main(ctx: Context) -> i32 {
  let result = ctx.fs~>exists("test.txt");
  let msg: string@View = match result {
    ok: bool => "exists"
  };
  return 0i32;
}
