# SPEC_COV: Pat-Exhaustive
// Complex exhaustiveness - all cases covered

public procedure main(ctx: Context) -> i32 {
  let b: bool = true
  let x: i32 = match b {
    true => 1,
    false => 0
  }
  return x
}
