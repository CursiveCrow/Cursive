# SPEC_COV: Index-Array-NonUsize
# E-TYP-1812: Array index expression has non-usize type.
# Using i32 as array index should fail.
public procedure main(ctx: Context) -> i32 {
  let arr: [i32; 3] = [1, 2, 3];
  let idx: i32 = 1;
  let x = arr[idx];
  return 0i32;
}
