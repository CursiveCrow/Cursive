# SPEC_COV: Chk-Subsumption-Modal-NonNiche
type Alias = Ptr<i32>@Valid

modal OptAlias {
  @Some { p: Alias }
  @None { }
}

public procedure main(ctx: Context) -> i32 {
  let a = OptAlias@Some { p: unsafe { transmute::<usize, Alias>(0usize) } };
  let b: OptAlias = move a;
  return 0;
}
