# SPEC_COV: P-Field-Record-Perm
record Rec {
  field: i32
}

public procedure main(ctx: Context) -> i32 {
  let r: unique Rec = unsafe { transmute::<Rec, unique Rec>(Rec { field: 1 }) };
  let f: unique i32 = move r.field;
  return 0;
}
