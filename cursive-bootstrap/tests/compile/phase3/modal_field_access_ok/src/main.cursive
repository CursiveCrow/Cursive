# SPEC_COV: T-Modal-Field
# SPEC_COV: T-Modal-Field-Perm
modal Mod { @Open { v: i32 } }

public procedure main(ctx: Context) -> i32 {
  let a = Mod@Open { v: 1 };
  let x = (move a).v;
  let b: const Mod@Open = unsafe { transmute::<i32, const Mod@Open>(0) };
  let y = (move b).v;
  return 0;
}
