# SPEC_COV: Dynamic-NonDispatchable
class ND { procedure bad(~, other: Self) -> i32 { return 0; } }
record R <: ND { v: i32 }

public procedure main(ctx: Context) -> i32 {
  let r = R { v: 0 };
  let d: $ND = r as $ND;
  return 0;
}
