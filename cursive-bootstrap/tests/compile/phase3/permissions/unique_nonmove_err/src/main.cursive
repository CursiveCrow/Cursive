# SPEC_COV: B-LetVar-UniqueNonMove-Err
# E-MEM-3007: unique binding from place expression requires explicit move.
# Must use move keyword for unique bindings from existing places.
record UniqueData {
  value: i32
}

public procedure main(ctx: Context) -> i32 {
  let data = UniqueData { value: 42 };
  let other: unique UniqueData = data;
  return 0i32;
}
