# SPEC_COV: Chk-Subsumption-Modal-NonNiche
modal OptPtr {
  @Some { p: const Ptr<i32>@Valid }
  @None { }
}

public procedure main(ctx: Context) -> i32 {
  let a = OptPtr@Some { p: unsafe { transmute::<usize, const Ptr<i32>@Valid>(0usize) } };
  let b: OptPtr = move a;
  return 0;
}
