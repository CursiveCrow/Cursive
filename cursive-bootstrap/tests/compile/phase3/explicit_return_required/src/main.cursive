# SPEC_COV: WF-ProcBody-ExplicitReturn-Err
# Test that procedures with non-unit return types must use explicit return
procedure implicit_return() -> i32 {
  let x: i32 = 42;
  x
}

public procedure main(ctx: Context) -> i32 {
  return 0;
}
