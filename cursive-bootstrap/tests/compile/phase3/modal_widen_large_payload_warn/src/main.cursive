# SPEC_COV: Warn-Widen-LargePayload
# SPEC_COV: T-Modal-Widen
modal Big {
  @Huge { v: [ [u8; 32]; 9] }
  @Empty { }
}

public procedure main(ctx: Context) -> i32 {
  let inner: [u8; 32] = [
    0u8, 0u8, 0u8, 0u8, 0u8, 0u8, 0u8, 0u8,
    0u8, 0u8, 0u8, 0u8, 0u8, 0u8, 0u8, 0u8,
    0u8, 0u8, 0u8, 0u8, 0u8, 0u8, 0u8, 0u8,
    0u8, 0u8, 0u8, 0u8, 0u8, 0u8, 0u8, 0u8
  ];
  let big: [ [u8; 32]; 9] = [
    inner, inner, inner, inner, inner, inner, inner, inner, inner
  ];
  let m = Big@Huge { v: big };
  let g: Big = widen (move m);
  0
}
