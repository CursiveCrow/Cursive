# SPEC_COV: Drop-Call-Err-Dyn
record R <: Drop { v: i32, procedure drop(~!) -> () { } }

public procedure main(ctx: Context) -> i32 {
  let r = R { v: 0 };
  let d: $Drop = r as $Drop;
  d~>drop();
  0
}
