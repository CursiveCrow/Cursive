// RUN: run-pass
// EXIT: 0
// SPEC: 13.2.11
// DESC: Verify calling convention for procedures

procedure add(a: i32, b: i32) -> i32 {
    return a + b
}

procedure sub(a: i32, b: i32) -> i32 {
    return a - b
}

public procedure main(move ctx: Context) -> i32 {
    let a = 10
    let b = 5
    let result = add(a, b)
    let c = 15
    let final_val = sub(result, c)
    return final_val
}
