// RUN: run-pass
// EXIT: 0
// SPEC: 6.4 Lower-Expr-Binary
// DESC: Verify binary operation lowering

public procedure main(move ctx: Context) -> i32 {
    let a: i32 = 20
    let b: i32 = 5

    let sum: i32 = a + b
    let diff: i32 = a - b
    let prod: i32 = a * b
    let quot: i32 = a / b
    let rem: i32 = a % b

    let ok: bool = sum == 25 && diff == 15 && prod == 100 && quot == 4 && rem == 0
    return if ok { 0 } else { 1 }
}
