// RUN: run-pass
// EXIT: 42
// SPEC: 5.2.1
// DESC: Union type creation and matching

public procedure main(move ctx: Context) -> i32 {
    let u: i32 | bool = 42
    let result: i32 = match u {
        v: i32 => { v }
        b: bool => { 0 }
    }
    return result
}
