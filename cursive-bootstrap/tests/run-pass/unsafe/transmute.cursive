// RUN: run-pass
// EXIT: 42
// SPEC: 5.2.18
// DESC: Transmute between same-size types

public procedure main(move ctx: Context) -> i32 {
    let x: u32 = 42
    let result: i32 = unsafe {
        transmute<i32>(x)
    }
    return result
}
