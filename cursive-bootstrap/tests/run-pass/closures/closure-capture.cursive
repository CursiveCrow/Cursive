// RUN: run-pass
// EXIT: 42
// SPEC: 5.2.4
// DESC: Procedure call with explicit environment parameter

procedure add_offset(x: i32, offset: i32) -> i32 {
    return x + offset
}

public procedure main(move ctx: Context) -> i32 {
    let offset: i32 = 32
    return add_offset(10, offset)
}
