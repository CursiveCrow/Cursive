// RUN: run-pass
// EXIT: 42
// SPEC: 5.2.6
// DESC: Result statement for expression block value

procedure compute() -> i32 {
    let x: i32 = {
        let a: i32 = 20
        let b: i32 = 22
        result a + b
    }
    return x
}

procedure conditional(flag: bool) -> i32 {
    let value: i32 = if flag {
        result 100
    } else {
        result 42
    }
    return value
}

public procedure main(move ctx: Context) -> i32 {
    let a: i32 = compute()
    let b: i32 = conditional(false)
    return b
}
