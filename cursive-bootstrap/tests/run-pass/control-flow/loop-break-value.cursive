// RUN: run-pass
// EXIT: 42
// SPEC: 5.2.6
// DESC: Loop with break returning value

public procedure main(move ctx: Context) -> i32 {
    let result: i32 = loop {
        break 42
    }
    return result
}
