// RUN: run-pass
// EXIT: 42
// SPEC: 5.2.17
// DESC: Nested regions

public procedure main(move ctx: Context) -> i32 {
    var result: i32 = 0
    region {
        let outer: i32 = ^10
        region {
            let inner: i32 = ^32
            result = outer + inner
        }
    }
    return result
}
