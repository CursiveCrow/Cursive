#include <cassert>

#include "cursive0/interpreter/interpreter.h"

int main() {
  cursive0::semantics::Sigma sigma;
  cursive0::semantics::InterpreterResult result;
  (void)sigma;
  (void)result;
  assert(true);
  return 0;
}
