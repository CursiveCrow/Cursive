#include <cassert>

#include "cursive0/eval/interpreter.h"

int main() {
  cursive0::eval::Sigma sigma;
  cursive0::eval::InterpreterResult result;
  (void)sigma;
  (void)result;
  assert(true);
  return 0;
}
