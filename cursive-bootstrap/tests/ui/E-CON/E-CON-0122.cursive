// RUN: compile-fail
// DIAG: E-CON-0122
// SPEC: 8.9
// DESC: Move of binding from outer parallel scope

record Resource {
    value: i32
}

procedure move_outer(domain: $ExecutionDomain) -> () {
    parallel domain {
        let r: Resource = Resource { value: 42 }
        parallel domain {
            spawn {
                let x: Resource = move r  //~ ERROR E-CON-0122: move from outer parallel
            }
        }
    }
    return ()
}

public procedure main(move ctx: Context) -> i32 {
    return 0
}
