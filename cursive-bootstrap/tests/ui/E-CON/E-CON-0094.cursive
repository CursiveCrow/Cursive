// RUN: compile-fail
// DIAG: E-CON-0094
// SPEC: 8.9
// DESC: speculative combined with release

record Counter {
    var value: i32
}

procedure speculative_release(c: shared Counter) -> () {
    # speculative release write c {  //~ ERROR E-CON-0094: speculative with release
        c.value = 10
    }
}

public procedure main(move ctx: Context) -> i32 {
    return 0
}
