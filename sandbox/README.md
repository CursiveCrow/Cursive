# Sandbox Notes

This folder is a scratch area and is **not** part of the automated test suite.

- Canonical semantics-oracle tests live under `cursive-bootstrap/tests/semantics_oracle/`.
- The project in `sandbox/drop_order_ok/` is a sandbox copy; the canonical
  version is `cursive-bootstrap/tests/semantics_oracle/drop_order_ok/`.
