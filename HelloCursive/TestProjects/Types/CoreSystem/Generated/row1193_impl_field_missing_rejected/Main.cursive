class RequiresStoredValue {
    value: i32
    procedure total(~) -> i32;
}

record MissingStoredValue <: RequiresStoredValue {
    procedure total(~) -> i32 {
        return 0
    }
}

public procedure main(move ctx: Context) -> i32 {
    let _ = ctx
    return 0
}
