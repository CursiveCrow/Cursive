modal Issue984PlainModal {
    @Ready {
        value: i32
    }
}

procedure make_ready() -> Issue984PlainModal@Ready {
    return Issue984PlainModal@Ready { value: 7 }
}

public procedure main(move ctx: Context) -> i32 {
    let _ = ctx
    let ready: Issue984PlainModal@Ready = make_ready()
    let general = widen(ready)
    let _ = general
    return 0
}
