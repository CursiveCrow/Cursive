public procedure main(move ctx: Context) -> i32 {
    let _ = ctx
    var gate: shared [i32; 4] = [0, 0, 0, 0]
    [[acquire, release]]
    # gate read {
        let observed: i32 = gate[0]
        let _ = observed
    }
    return 0
}
