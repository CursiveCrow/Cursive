public procedure runGroup(ctx: Context) -> i32 {
    "section-start"

    let status_modal_diagnostic_matrix: i32 = RunModalDiagnosticMatrix(ctx)
    if (status_modal_diagnostic_matrix != 0) {
        "section-end"
        return status_modal_diagnostic_matrix
    }

    "section-end"
    return 0
}
