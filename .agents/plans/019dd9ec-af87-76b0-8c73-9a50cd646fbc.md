# Oracle Spec Marker Continuation

## Objective

- Continue adding machine-readable `CURSIVE-SPEC-UNIT` oracle parsing markers to
  `docs/CursiveSpecification.md`.
- Preserve the specification's normative UTF-8 content exactly while making the
  next unmarked compiler/runtime obligations parseable for the CursiveOracle.

## Success Criteria

- Chapter 13 formal rules, grammar blocks, helper definitions, semantic
  requirements, lowering requirements, and diagnostics are wrapped in oracle
  markers at atomic or smallest coherent definition-family boundaries.
- No labelled formal judgement block remains unmarked in Chapter 13.
- Marker open/close counts remain balanced and marker ids are unique.
- The edited spec remains valid UTF-8 and Chapter 13 line endings remain CRLF.

## Source Tree

- path: docs/CursiveSpecification.md
  owns:
  - authoritative Cursive language specification text and oracle marker metadata
- path: .agents/plans/019dd9ec-af87-76b0-8c73-9a50cd646fbc.md
  owns:
  - current session write authority and completion evidence for this marker pass

## Technical Stack

- Markdown specification with embedded formal Unicode judgement notation.
- `CURSIVE-SPEC-UNIT` HTML comment markers consumed by the oracle ingestion path.
- Shell, `rg`, `awk`, and targeted Python byte-level inspection for verification.

## Constraints

- Do not change Cursive semantics.
- Do not edit or normalize existing normative spec content.
- Do not ASCII-normalize Unicode judgement, inference, or type-theory symbols.
- Do not add compatibility shims, parallel sources of truth, or generated spec
  replacements.
- Insert marker comments only.

## Non-Goals

- Do not implement compiler behavior in this task.
- Do not alter CursiveOracle source code in this task.
- Do not mark chapters after Chapter 13 in this bounded continuation slice.
- Do not claim the full specification marker pass is complete.

## References

- Current user request to continue adding oracle parsing markers.
- `docs/CursiveSpecification.md` Chapter 13, `Modal and Special Types`.
- `.agents/DIRECTIVES.md`.

## Verification Strategy

- python3 -c "from pathlib import Path; Path('docs/CursiveSpecification.md').read_bytes().decode('utf-8'); print('utf8 ok')"
- python3 -c "from pathlib import Path; b=Path('docs/CursiveSpecification.md').read_bytes(); s=b.count(b'<!-- CURSIVE-SPEC-UNIT'); e=b.count(b'<!-- /CURSIVE-SPEC-UNIT -->'); print(s,e,'balanced' if s==e else 'unbalanced')"
- awk 'BEGIN{m=0} /<!-- CURSIVE-SPEC-UNIT/{m=1} /<!-- \\/CURSIVE-SPEC-UNIT/{m=0; next} /^\\*\\*\\([^)]*\\)\\*\\*/{if(m==0 && NR>=34837 && NR<36578) print NR ":" $0}' docs/CursiveSpecification.md
- python3 -c "from pathlib import Path; lines=Path('docs/CursiveSpecification.md').read_bytes().splitlines(keepends=True); print({('CRLF' if l.endswith(b'\\r\\n') else 'LF' if l.endswith(b'\\n') else 'NONE') for l in lines[34836:36577]})"
- git diff --check -- docs/CursiveSpecification.md .agents/plans/019dd9ec-af87-76b0-8c73-9a50cd646fbc.md

## Tasks

### TASK-001: Mark Chapter 13 oracle units
Status: complete
Kind: root-cause-change
Observed problem:
- The oracle marker pass currently stops after Chapter 12, leaving Chapter 13
  compiler/runtime obligations unwrapped and therefore not mechanically
  extractable as spec units.
Required behavior:
- Chapter 13 obligations must be wrapped in parseable `CURSIVE-SPEC-UNIT`
  markers without changing the underlying normative text.
Source of truth:
- User request and `docs/CursiveSpecification.md`.
Canonical owner:
- docs/CursiveSpecification.md
Why this is the owner:
- The specification is the authoritative source of truth for Cursive conformance
  obligations, and the oracle consumes markers from this document directly.
Downstream consumers:
- CursiveOracle spec ingestion and future conformance case generation consume the
  marker metadata.
Forbidden implementation paths:
- Do not change compiler/runtime behavior.
- Do not add a duplicate generated specification file.
- Do not rewrite the spec text to make marking easier.
- Do not normalize Unicode or line endings in the existing spec content.
Rejected shortcuts:
- Marking only labelled rules | misses grammar, helper definitions, diagnostics,
  and normative prose obligations.
- Wrapping the whole chapter in one marker | not atomic enough for oracle case
  generation.
Allowed writes:
- path: docs/CursiveSpecification.md
  role: canonical-owner
  permitted:
  - insert oracle marker comments around Chapter 13 obligations
  forbidden:
  - alter normative specification content or Unicode symbols
- path: .agents/plans/019dd9ec-af87-76b0-8c73-9a50cd646fbc.md
  role: plan
  permitted:
  - update task status and completion evidence
  forbidden:
  - authorize unrelated implementation edits
Verification:
- python3 -c "from pathlib import Path; Path('docs/CursiveSpecification.md').read_bytes().decode('utf-8'); print('utf8 ok')"
- python3 -c "from pathlib import Path; b=Path('docs/CursiveSpecification.md').read_bytes(); s=b.count(b'<!-- CURSIVE-SPEC-UNIT'); e=b.count(b'<!-- /CURSIVE-SPEC-UNIT -->'); print(s,e,'balanced' if s==e else 'unbalanced')"
- awk 'BEGIN{m=0} /<!-- CURSIVE-SPEC-UNIT/{m=1} /<!-- \\/CURSIVE-SPEC-UNIT/{m=0; next} /^\\*\\*\\([^)]*\\)\\*\\*/{if(m==0 && NR>=34837 && NR<36578) print NR ":" $0}' docs/CursiveSpecification.md
- python3 -c "from pathlib import Path; lines=Path('docs/CursiveSpecification.md').read_bytes().splitlines(keepends=True); print({('CRLF' if l.endswith(b'\\r\\n') else 'LF' if l.endswith(b'\\n') else 'NONE') for l in lines[34836:36577]})"
- git diff --check -- docs/CursiveSpecification.md .agents/plans/019dd9ec-af87-76b0-8c73-9a50cd646fbc.md
Action: Insert Chapter 13 oracle markers, then run the listed verification checks.
Completion evidence: Chapter 13 markers inserted and verified; see evidence bullets below.
- Changed `docs/CursiveSpecification.md` by inserting Chapter 13 `CURSIVE-SPEC-UNIT`
  marker comments only. Chapter 13 now has 275 units: 11 grammar, 182 formal-rule,
  46 formal-definition, 23 semantic-requirement, 12 diagnostic-requirement, and
  1 diagnostic-table.
- Verified UTF-8 decode: `utf8 ok`.
- Verified global marker balance: `2194 2194 balanced`.
- Verified planned labelled-rule audit produced no output for the original Chapter
  13 line window.
- Verified original Chapter 13 line-window endings remain `{'CRLF'}`.
- Verified dynamic Chapter 13 boundaries are lines `34837` to `39508`, with line
  endings `{'CRLF'}`.
- Verified dynamic Chapter 13 labelled-rule audit: `unmarked_ch13_labels 0`.
- Verified dynamic Chapter 13 formal/normative candidate audit:
  `unmarked_ch13_formalish 0`.
- Verified Chapter 13 marker ids are unique: `ch13 ids 275 unique 275 dupes []`.
- Ran `git diff --check -- docs/CursiveSpecification.md .agents/plans/019dd9ec-af87-76b0-8c73-9a50cd646fbc.md`; it exited 0 with Git's existing CRLF warning for
  `docs/CursiveSpecification.md`.
